{
  "_comment": "Not enabled yet (see data/locales.json). Empty on purpose - the t() helper falls back to en.json for any missing key, so this locale can be filled in incrementally without breaking anything."
}
